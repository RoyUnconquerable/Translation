# Translation project bootstrap

This repository has one active translation pipeline: `chatgpt/`.

Read `AGENTS.md`, then `chatgpt/PROJECT_INSTRUCTIONS.md`.

The legacy `project/`, `source/`, `work/` and `output/` paths are read-only.
Their original state is preserved on `archive/claude-pipeline-2026-08-19` at
commit `405e61125ba6abc935cbc56eaf1ccbf57c3fb091`.
